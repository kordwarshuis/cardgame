module.exports = {

  // https://cli.vuejs.org/config/#publicpath

  //=============
  // if your app will run from a sub directory then this is the path to the sub directory
  publicPath: process.env.NODE_ENV === 'production' ? '/path/to/app/' : '/',
  //=============

  lintOnSave: false,
  css: {
    loaderOptions: {
      sass: {
        prependData: '@import "@/assets/css/_variables.scss";'
      }
    }
  },
  devServer: {//https://github.com/vuejs/vue-cli/issues/2051#issuecomment-420320469
    watchOptions: {
      // poll: true
    }
  }
};